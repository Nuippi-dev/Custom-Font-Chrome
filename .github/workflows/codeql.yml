name: "CodeQL Analysis"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '24 0 * * 5' # Weekly scan every Friday at 00:24 UTC

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    
    permissions:
      security-events: write
      actions: read
      contents: read
      packages: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: javascript-typescript
            build-mode: none
          - language: python
            build-mode: none
          - language: java-kotlin
            build-mode: manual
          - language: c-cpp
            build-mode: manual

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Initialize CodeQL
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: +security-extended,security-and-quality

      # Step 3: Manual build (only for specific languages like c-cpp and java-kotlin)
      - if: matrix.build-mode == 'manual'
        name: Manual Build
        shell: bash
        run: |
          echo "Building for manual languages..."
          if [ "${{ matrix.language }}" = "java-kotlin" ]; then
            ./gradlew build
          elif [ "${{ matrix.language }}" = "c-cpp" ]; then
            make
          else
            echo "No build commands defined!"
            exit 1

      # Step 4: Perform CodeQL Analysis
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"

      # Step 5: Send email notification if CodeQL analysis fails
      - name: Notify on Failure
        if: failure() # This block runs ONLY if the job fails
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }} # Use the secrets you set up
          password: ${{ secrets.EMAIL_PASSWORD }} # Use the secrets you set up
          subject: "CodeQL Analysis Failed"
          body: |
            CodeQL analysis has failed for ${{ matrix.language }}.
            Please check the security alerts in the repository.
          to: your-email@example.com  # Replace this with your recipient email
          from: CodeQL Bot <bot@example.com>
